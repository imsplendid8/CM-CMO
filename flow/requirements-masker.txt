# 캡쳐 자동 마스킹(masker.py) 의존성
# 기본 엔진: EasyOCR (한국어 정확도 우수, 최초 실행 시 모델 자동 다운로드)
easyocr>=1.7.0
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# 가벼운 대안 엔진(Tesseract) 사용 시 추가로 필요:
#   pip install pytesseract
#   그리고 OS에 tesseract 바이너리 + 한국어 데이터(kor) 설치:
#     macOS:  brew install tesseract tesseract-lang
#     Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-kor
#     Windows: https://github.com/UB-Mannheim/tesseract/wiki
# pytesseract>=0.3.10
